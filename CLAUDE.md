When creating or editing any markdown file in this repo, check and update it against the writing standards in `System-Automation/context-management.md` (see `### When Editing Existing Files` and `### Templates`).

This is a personal repository with no code — only markdown files and configuration. Push all changes directly to the main branch. Do not create feature branches or pull requests.
